class Client {
    id: number;
    name: string;
    point: number;

    constructor(id: number, name: string, point: number) {
        this.id = id;
        this.name = name;
        this.point = point;
        
    }
}

export default Client;